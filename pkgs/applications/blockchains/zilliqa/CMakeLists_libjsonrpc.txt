diff --git a/cmake/InstallJsonrpccpp.cmake b/cmake/InstallJsonrpccpp.cmake
index a4d24e66a..cdf2f74de 100644
--- a/cmake/InstallJsonrpccpp.cmake
+++ b/cmake/InstallJsonrpccpp.cmake
@@ -48,8 +48,7 @@ ExternalProject_Add(jsonrpc-project
     PREFIX src/depends/jsonrpc
     URL https://github.com/cinemast/libjson-rpc-cpp/archive/v1.2.0.tar.gz
     URL_HASH SHA256=485556bd27bd546c025d9f9a2f53e89b4460bf820fd5de847ede2539f7440091
-    # On Windows it tries to install this dir. Create it to prevent failure.
-    PATCH_COMMAND cmake -E make_directory <SOURCE_DIR>/win32-deps/include
+    PATCH_COMMAND sed -i -re "s|MATCHES \"jsoncpp\"|MATCHES \".*/jsoncpp/json$\"|g" <SOURCE_DIR>/cmake/FindJsoncpp.cmake
     CMAKE_ARGS ${CMAKE_ARGS}
     # overwrite build and install commands to force Release build on MSVC.
     BUILD_COMMAND cmake --build <BINARY_DIR> --config Release
